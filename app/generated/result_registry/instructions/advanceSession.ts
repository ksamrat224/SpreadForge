/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { RESULT_REGISTRY_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const ADVANCE_SESSION_DISCRIMINATOR = new Uint8Array([
  44, 179, 239, 129, 67, 34, 6, 236,
]);

export function getAdvanceSessionDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADVANCE_SESSION_DISCRIMINATOR,
  );
}

export type AdvanceSessionInstruction<
  TProgram extends string = typeof RESULT_REGISTRY_PROGRAM_ADDRESS,
  TAccountActor extends string | AccountMeta<string> = string,
  TAccountSession extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountActor extends string
        ? ReadonlySignerAccount<TAccountActor> &
            AccountSignerMeta<TAccountActor>
        : TAccountActor,
      TAccountSession extends string
        ? WritableAccount<TAccountSession>
        : TAccountSession,
      ...TRemainingAccounts,
    ]
  >;

export type AdvanceSessionInstructionData = {
  discriminator: ReadonlyUint8Array;
  tick: number;
  stateHash: ReadonlyUint8Array;
};

export type AdvanceSessionInstructionDataArgs = {
  tick: number;
  stateHash: ReadonlyUint8Array;
};

export function getAdvanceSessionInstructionDataEncoder(): FixedSizeEncoder<AdvanceSessionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tick", getU16Encoder()],
      ["stateHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: ADVANCE_SESSION_DISCRIMINATOR }),
  );
}

export function getAdvanceSessionInstructionDataDecoder(): FixedSizeDecoder<AdvanceSessionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tick", getU16Decoder()],
    ["stateHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getAdvanceSessionInstructionDataCodec(): FixedSizeCodec<
  AdvanceSessionInstructionDataArgs,
  AdvanceSessionInstructionData
> {
  return combineCodec(
    getAdvanceSessionInstructionDataEncoder(),
    getAdvanceSessionInstructionDataDecoder(),
  );
}

export type AdvanceSessionInput<
  TAccountActor extends string = string,
  TAccountSession extends string = string,
> = {
  actor: TransactionSigner<TAccountActor>;
  session: Address<TAccountSession>;
  tick: AdvanceSessionInstructionDataArgs["tick"];
  stateHash: AdvanceSessionInstructionDataArgs["stateHash"];
};

export function getAdvanceSessionInstruction<
  TAccountActor extends string,
  TAccountSession extends string,
  TProgramAddress extends Address = typeof RESULT_REGISTRY_PROGRAM_ADDRESS,
>(
  input: AdvanceSessionInput<TAccountActor, TAccountSession>,
  config?: { programAddress?: TProgramAddress },
): AdvanceSessionInstruction<TProgramAddress, TAccountActor, TAccountSession> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RESULT_REGISTRY_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    actor: { value: input.actor ?? null, isWritable: false },
    session: { value: input.session ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.actor),
      getAccountMeta(accounts.session),
    ],
    data: getAdvanceSessionInstructionDataEncoder().encode(
      args as AdvanceSessionInstructionDataArgs,
    ),
    programAddress,
  } as AdvanceSessionInstruction<
    TProgramAddress,
    TAccountActor,
    TAccountSession
  >);
}

export type ParsedAdvanceSessionInstruction<
  TProgram extends string = typeof RESULT_REGISTRY_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    actor: TAccountMetas[0];
    session: TAccountMetas[1];
  };
  data: AdvanceSessionInstructionData;
};

export function parseAdvanceSessionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdvanceSessionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { actor: getNextAccount(), session: getNextAccount() },
    data: getAdvanceSessionInstructionDataDecoder().decode(instruction.data),
  };
}
